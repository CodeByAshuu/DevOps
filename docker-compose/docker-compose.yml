version: "3.9"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db
    command: npm start
  db:
    images: mysql:18
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db1
    volumes:
      - myvol: /var/lib/mysql
volumes:
  - myvol:
    